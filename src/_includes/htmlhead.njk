<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="/css/stylesheet.css">
</head>
<body>
<header>
<!-- page header -->
<nav>
<style>
  .nav-item-active {
  font-weight: bold;
  }
</style>
<!-- navigation -->
{{ collections.all
   | eleventyNavigation
   | eleventyNavigationToHtml({ activeKey: eleventyNavigation.key,
                                listItemClass: "nav-item",
                                activeListItemClass: "nav-item-active"})
   | safe }}
</nav>

{% set crumbs = collections.all
   | eleventyNavigationBreadcrumb(eleventyNavigation.key) %}
<div class="crumbs">
{% for crumb in crumbs -%}
  <a class="crumb" href="{{ crumb.url | url }}">{{ crumb.title }}</a> /
{% endfor %}
<span class="last-crumb">{{ eleventyNavigation.title }}</span>

{{ headercontent }}
</header>

<hr>
