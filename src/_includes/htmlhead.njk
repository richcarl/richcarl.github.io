<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="/css/stylesheet.css">
</head>
<body>
<div class=main-grid>
<!-- page header -->
<header>
  <!-- top level menu -->
  <div id="topmenu">
    <input type="checkbox" id="topmenu-toggle"><label for="topmenu-toggle"><span id="topmenu-burger">E</span><span id="topmenu-close">X</span></label>
    {% set menuPages = collections.all | eleventyNavigation -%}
    <ul id="topmenu-list">
      {%- for entry in menuPages %}
      <li{% if entry.url == page.url %} class="topmenu-item-active"{% else %} class="topmenu-item"{% endif %}>
        <a href="{{ entry.url | url }}">{{ entry.title }}</a>
      </li>
      {%- endfor %}
    </ul>
  </div>
{{ headercontent }}
</header>
<div class=left-column>
<nav>
<!-- navigation -->
{{ collections.all
   | eleventyNavigation
   | eleventyNavigationToHtml({ activeKey: eleventyNavigation.key,
                                listClass: "nav-list",
                                anchorClass: "nav-anchor",
                                activeAnchorClass: "nav-anchor-active",
                                listItemClass: "nav-item",
                                activeListItemClass: "nav-item-active"})
   | safe }}
</nav>
</div>
<div class=right-column>
</div>
